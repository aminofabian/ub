# Resend (https://resend.com) — copy to `.env.resend` (gitignored) or set in your host env.
# Do not commit API keys. Rotate any key that was pasted into chat or tickets.

# Required. Dashboard → API Keys.
RESEND_API_KEY=re_VpWBgK45_Jx6vxC2Cc5TtcmTuiZJP6sfr

# Verified sending domain (used to build default From: UB <noreply@RESEND_DOMAIN>).
RESEND_DOMAIN=palmart.co.ke

# Optional: overrides default From (must be allowed by Resend for your domain), e.g.
# RESEND_FROM=PalMart <hello@palmart.co.ke>
